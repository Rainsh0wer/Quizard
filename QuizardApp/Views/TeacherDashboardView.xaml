<UserControl x:Class="QuizardApp.Views.TeacherDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QuickActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357ABD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Welcome Section -->
            <Border Style="{StaticResource StatCardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="{Binding WelcomeMessage}" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="#2C3E50"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Here's what's happening in your classrooms today" 
                               FontSize="14" 
                               Foreground="#7F8C8D"/>
                </StackPanel>
            </Border>

            <!-- Statistics Cards -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="📝" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding TotalQuizzes}" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="#4A90E2" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Quizzes" 
                                   FontSize="12" 
                                   Foreground="#7F8C8D" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="👥" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding TotalStudents}" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="#27AE60" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Students" 
                                   FontSize="12" 
                                   Foreground="#7F8C8D" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🏫" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding TotalClassrooms}" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="#E67E22" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Classrooms" 
                                   FontSize="12" 
                                   Foreground="#7F8C8D" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="📊" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding AverageScore}" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="#9B59B6" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Avg Score" 
                                   FontSize="12" 
                                   Foreground="#7F8C8D" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Quick Actions -->
            <Border Style="{StaticResource StatCardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Quick Actions" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#2C3E50" 
                               Margin="0,0,0,15"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" 
                                Style="{StaticResource QuickActionButtonStyle}"
                                Command="{Binding ShowQuizzesCommand}">
                            <StackPanel>
                                <TextBlock Text="📝" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="Create Quiz" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="1" 
                                Style="{StaticResource QuickActionButtonStyle}"
                                Command="{Binding ShowClassroomsCommand}">
                            <StackPanel>
                                <TextBlock Text="🏫" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="Create Classroom" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="2" 
                                Style="{StaticResource QuickActionButtonStyle}"
                                Command="{Binding ShowResultsCommand}">
                            <StackPanel>
                                <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="View Results" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Recent Quizzes -->
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Recent Quizzes" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#2C3E50" 
                               Margin="0,0,0,15"/>
                    
                    <ListView ItemsSource="{Binding RecentQuizzes}" 
                              Background="Transparent" 
                              BorderThickness="0"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA" 
                                        CornerRadius="5" 
                                        Padding="15" 
                                        Margin="0,5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Title}" 
                                                       FontWeight="SemiBold" 
                                                       FontSize="14" 
                                                       Foreground="#2C3E50"/>
                                            <TextBlock Text="{Binding Subject.Name}" 
                                                       FontSize="12" 
                                                       Foreground="#7F8C8D" 
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding CreatedAt, StringFormat='MMM dd, yyyy'}" 
                                                   FontSize="12" 
                                                   Foreground="#7F8C8D" 
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>